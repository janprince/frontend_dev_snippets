/**
 * prop is a special react object that works in a similar way to a JavaScript object, and that its properties can be accessed by dot notation. 
 * 
 * You also learned why developers use props in order to make their apps more dynamic and flexible. 
 * 
 * Finally, you examined some of the limitations about using props in that you cannot use them to send data back to a parent component, 
 * and the functions using props must never modify its own props.
 * 
 */